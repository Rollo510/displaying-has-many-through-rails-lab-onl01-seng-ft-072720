<h1> Appointment Information: </h1>

<ul>

<li> <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %> <%= @appointment.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %></li>
<li> <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %> <%= @appointment.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %></li>

</ul>